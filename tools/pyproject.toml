[project]
name = "lef-web-tools"
version = "0.1.0"
description = "A set of tools used to do nice things in order to produce the lef.fyi static website"
authors = [{name = "Lef Filippakis", email = "Lef-F@users.noreply.github.com"}]
requires-python = ">=3.9"

[dependency-groups]
dev = ["ruff"]
